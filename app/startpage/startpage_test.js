'use strict';

describe('myApp.startpage module', function() {

  beforeEach(module('myApp.startpage'));

  describe('startpage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var startpageCtrl = $controller('StartPageCtrl');
      expect(startpageCtrl).toBeDefined();
    }));

  });
});